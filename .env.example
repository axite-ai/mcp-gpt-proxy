# MCP GPT Proxy Configuration

# Required: URL of the upstream MCP server to proxy requests to
MCP_SERVER_URL=http://localhost:3001/mcp

# Optional: Port for the proxy server (defaults to 3000)
# PORT=3000

# Optional: Environment (development, production)
# NODE_ENV=development
